#include "RadixTree.hpp"

using namespace RadixTree;

template <typename T>
__global__ void makeCodes(T* x_vals, T* y_vals, T* z_vals, Node* nodes, size_t N) {
    
}

template <typename T>
RadixTree::RadixTree(const PointCloud<T>& cloud) {

}